import { Form } from '../form/Form'
import styles from './Auth.module.scss'

export const Auth = () => {

    return (
        <div className={styles.login}>
            <Form />
        </div>

    )
}